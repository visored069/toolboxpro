.vercel
.env*

# Agent/skill setup (keep configs + skill docs, ignore heavy binaries & local settings)
.claude/settings.local.json
.claude/skills/impeccable/scripts/bin/

# Local admin dashboard (never deploy)
registry-dashboard.html
link-report.md
