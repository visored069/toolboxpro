{
  "name": "ai-directories",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node lint-registry.js && node gen-sitemap.js && node check-links.js && echo 'static site: checks passed, no build step required'",
    "lint:data": "node lint-registry.js",
    "lint:links": "node check-links.js"
  },
  "dependencies": {}
}
