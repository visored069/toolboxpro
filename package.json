{
  "name": "toolboxpro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cp -r . /vercel/output/ && rm -rf /vercel/output/node_modules /vercel/output/.vercel /vercel/output/.freebuff /vercel/output/deploy /vercel/output/.git 2>/dev/null; echo done"
  }
}
